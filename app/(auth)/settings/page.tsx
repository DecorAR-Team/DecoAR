import AccountSettings from "@/app/ui/settings/accountSettings";
import Logout from "@/app/ui/settings/logout";

export default function Settings () {

  return (
    <div>
        <AccountSettings />
        <Logout />
    </div>
  );
}